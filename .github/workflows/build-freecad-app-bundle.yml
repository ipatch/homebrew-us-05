---

name: Build FreeCAD MacOS app bundle

on:
  workflow_dispatch:

env:
  HOMEBREW_NO_ANALYTICS: 1
  HOMEBREW_NO_AUTO_UPDATE: 1
  HOMEBREW_NO_INSTALL_CLEANUP: 1
  HOMEBREW_NO_INSTALL_FROM_API: 1
  HOMEBREW_DEVELOPER: 1

jobs:
  build:
    strategy:
      fail-fast: false
      max-parallel: 1
      matrix:
        os:
          - macos-14
          # - macos-13
          # - macos-12

    runs-on: ${{ matrix.os }}

    timeout-minutes: 1200

    steps:
      - name: set foos
        id: set-foos
        run: |
          echo "foo=bar" >> "$GITHUB_OUTPUT"

      - name: print foos
        id: print-foos
        env:
          foo: ${{ steps.set-foos.outputs.foo }}
        run: echo "the value of foo is $foo"

      - name: set brew prefix & cache paths
        id: set-brew-prefix-and-cache-paths
        run: |
           bp=$(brew --prefix)
           hbc="$bp/cache"
           echo "bp=\"$bp\"" >> "$GITHUB_OUTPUT"
           echo "hbc=\"$hbc\"" >> "$GITHUB_OUTPUT"
           echo "bp=\"$bp\"" >> "$GITHUB_ENV"
           echo "hbc=\"$hbc\"" >> "$GITHUB_ENV"

      - name: Print Brew Prefix & Cache paths
        id: print-brew-prefix-cache-paths
        env:
          bp: ${{ steps.set-brew-prefix-and-cache-paths.outputs.bp }}
          hbc: ${{ steps.set-brew-prefix-and-cache-paths.outputs.hbc }}
        run: |
          echo "the value of bp is set to $bp"
          echo "the value of hbc is set to $hbc"

      - name: Ensure cache directory exists
        run: |
          mkdir -p ${{ env.hbc }}
          ls -la ${{ env.hbc }}

      - name: Print current version of Homebrew and update
        run: |
          brew -v;
          brew update;
          brew -v;

      - name: Remove preinstalled Homebrew packages
        run: |
          brew list --formula | xargs brew uninstall --force;
          brew list --cask | xargs brew uninstall --cask --force;
          # brew cleanup

      - name: setup freecad tap
        id: setup-freecad-tap
        run: |
          brew tap -v freecad/freecad

      - name: install freecad deps
        id: install-freecad-deps
        uses: tecolicom/actions-install-and-cache@v1
        with:
          run: brew install --only-dependencies --formula -v freecad/freecad/freecad
          path: ${{ env.bp }}
          cache: yes
          key: ${{ runner.os }}-freecad-deps-${{ github.run_id }}-${{ hashFiles('**/Brewfile') }}
          verbose: true

      # NOWORK!
      # - name: save homebrew package cache
      #   id: save-homebrew-package-cache
      #   uses: actions/cache@v4
      #   with:
      #     path: ${{ env.hbc }}
      #     # NOWORK!
      #     # key: ${{ runner.os }}-cache-homebrew-packages-${{ hashFiles('**/Brewfile') }}
      #     # NOWORK!
      #     # key: ${{ runner.os }}-cache-homebrew-packages-${{ hashFiles('$HOME/Brewfile') }}
      #     restore-keys: |
      #       ${{ runner.os}}-cache-homebrew-packages

      # TODO: fixme, this caching is not working
      # - name: Cache Homebrew Packages
      #   id: cache-homebrew-packages
      #   uses: actions/cache@v4
      #   with:
      #     path: ${{ steps.set-brew-prefix-and-cache-paths.outputs.bp }}
      #     key: ${{ runner.os }}-cache-homebrew-packages-${{ hashFiles('$HOME/Brewfile') }}
      #     restore-keys: |
      #       ${{ runner.os }}-cache-homebrew-packages
      #
      # - name: List the state of the brew packages
      #   if: ${{ steps.cache-homebrew-packages.outputs.cache-hit != 'true' }}
      #   continue-on-error: true
      #   run: brew list

      # - name: List files in Homebrew Cellar
      #   env:
      #     bp: ${{ steps.set-brew-prefix-and-cache-paths.outputs.bp }}
      #   run: |
      #     ls -la $bp/Cellar


      # - name: checkout repository
      #   uses: actions/checkout@v2

      # - name: intentionally fail
      #   run: exit 1

      - name: debug with tmate on failure
        if: ${{ failure() }}
        uses: mxschmitt/action-tmate@v3      

      # - name: debug with lhotari/action-upterm
      #   id: debug-workflow-run
      #   # uses: actions/checkout@v2
      #   uses: lhotari/action-upterm@v1
      #   if: ${{ failure() }}
      #   with:
      #     # limit ssh access to user who triggered the workflow
      #     limit-access-to-actor: true
      #     # shut down server after 10 minutes if no one connects
      #     wait-timeout-minutes: 10

